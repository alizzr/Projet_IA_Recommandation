server {
    listen 80;
    server_name localhost;

    # Répertoire où React sera servi
    root /usr/share/nginx/html;
    index index.html;

    # Route toutes les URL vers React (SPA)
    location / {
        try_files $uri /index.html;
    }

    # ---- PROXY VERS LES DIFFÉRENTS MICROSERVICES ----

    # Product service (port 5002)
    location /api/products/ {
        proxy_pass http://product_service:5002/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Recommendation service (port 5001)
    location /api/recommendations/ {
        proxy_pass http://recommendation_service:5001/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # User service (port 5003)
    location /api/users/ {
        proxy_pass http://user_service:5003/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
